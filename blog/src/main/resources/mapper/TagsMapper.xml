<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shiyi.mapper.TagsMapper">

    <delete id="deleteArticleToTags">
        DELETE FROM b_article_tag WHERE article_id in
            <foreach collection="ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
    </delete>

    <select id="findByArticleId" resultType="java.lang.Long">
        select tag_id from b_article_tag where article_id=#{articleId}
    </select>

    <select id="getTagsName" resultType="java.lang.String">
        select name from b_tags where id in(select tag_id from b_article_tag where article_id=#{articleId})
    </select>

    <select id="findByTagId" resultType="java.lang.Long">
        select article_id from b_article_tag where tag_id=#{tagId}
    </select>

    <select id="findByArticleIdToTags" resultType="com.shiyi.entity.Tags">
        select id,name from b_tags where id in (select tag_id from b_article_tag where article_id=#{articleId})
    </select>

    <select id="countArticle" resultType="java.lang.Integer">
        select count(id) from b_article_tag where tag_id=#{id}
    </select>

    <insert id="saveArticleToTags">
        insert ignore into  b_article_tag(article_id,tag_id) values
        <foreach collection="tags" item="tagsId" index="index" separator=",">
            ( #{articleId},#{tagsId})
        </foreach>
    </insert>
</mapper>
