server:
  port: 8800
  servlet:
    # 应用的访问路径
    context-path: /shiyi
spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  application:
    name: shiyiblog
  #  ssl:
  #    key-store: classpath:6276737_www.isblog.com.cn.pfx #拷贝到根目录的证书文件
  #    key-store-password: 5Ec4QJBK #密码(下载tomcat证书压缩包里的txt文件)
  #    key-store-type: PKCS12       #固定写法
  datasource:
    url: ENC(5pNy/o25caqB5tDZva1UcRO45LCq4NavYzL5MdbHFHtIjeMnwVC90vPeMEuJSW2R++Q3pn2xr3C0gttCTL6dne4a+ndgo5al7BHdjgeBVFF6cDvtOfD10v7YUly/Mioju5UkUkSM5D6f5hEJTCI7m04+els+f4aqyTSo87BkAG3NHcFBa7E5CjqaMTwmHamqOZSY558gilE=)
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
    type: com.alibaba.druid.pool.DruidDataSource
  elasticsearch:
    rest:
      uris: localhost:9200
  redis:
    host: 42.192.150.44
    port: 6379
    timeout: 3000
    password:

jasypt:
  encryptor:
    password: RequiredEncryptionconfigurationpropertymissing

#==============================线程池配置==============================
task:
  executor:
    core_pool_size: 10
    max_pool_size: 200
    queue_capacity: 1200
    keep_alive_seconds: 60

#==============================Sa-Token配置==============================
sa-token:
  # token名称 (同时也是cookie名称)
  token-name: Authorization
  # token有效期，单位s 默认30天, -1代表永不过期
  timeout: 3600
  # token临时有效期 (指定时间内无操作就视为token过期) 单位: 秒
  activity-timeout: -1
  # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时新登录挤掉旧登录)
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个token (为true时所有登录共用一个token, 为false时每次登录新建一个token)
  is-share: false
  # token风格
  token-style: uuid
  # 是否输出操作日志
  is-log: false

#==============================mybatis-plus配置==============================
mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
  #实体扫描，多个package用逗号或者分号分隔
  typeAliasesPackage: com.shiyi.entity
  global-config:
    # 数据库相关配置
    db-config:
      #主键类型  AUTO:"数据库ID自增", INPUT:"用户输入ID",ID_WORKER:"全局唯一ID (数字类型唯一ID)", UUID:"全局唯一ID UUID";
      id-type: AUTO
      #字段策略 IGNORED:"忽略判断",NOT_NULL:"非 NULL 判断"),NOT_EMPTY:"非空判断"
      field-strategy: not_empty
      #驼峰下划线转换
      column-underline: true
      db-type: mysql
    #刷新mapper 调试神器
    refresh: true
  # 原生配置
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
  logging:
    level: warn

#==============================knife4j配置=======================================
knife4j:
  # 开启增强配置
  enable: true
  basic:
    enable: true
    # Basic认证用户名
    username: admin
    # Basic认证密码
    password:

#============================第三方登录配置信息===================================
qq:
  app-id:
  check-token-url: https://graph.qq.com/oauth2.0/me?access_token={access_token}
  user-info-url: https://graph.qq.com/user/get_user_info?openid={openid}&access_token={access_token}&oauth_consumer_key={oauth_consumer_key}
weibo:
  app-id:
  app-secret:
  grant-type: authorization_code
  redirect-url: http://www.shiyit.com/callback/weibo
  access-token-url: https://api.weibo.com/oauth2/access_token
  user-info-url: https://api.weibo.com/2/users/show.json?uid={uid}&access_token={access_token}
gitee:
  app-id:
  app-secret:
  grant-type: authorization_code
  redirect-url: http://www.shiyit.com/callback/gitee
  access-token-url: https://gitee.com/oauth/token
  user-info-url: https://gitee.com/api/v5/user?access_token={access_token}
baidu:
  url:
file:
  upload-folder: /usr/local/blog/file/